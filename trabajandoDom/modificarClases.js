

// /* para cambiar una clase */

// creamos la variable y escribimos entre los corchetes el 'id'
// var segundoParrafo = document.getElementById('segundo');

// añadimos la nueva clase
// segundoParrafo.className = 'texto fondo-rojo';

// añadimos un nuevo 'id'

// declaramos la variable y seleccionamos el elemento al cual se va a añadir el 'id'
// var primerParrafo = document.getElementsByClassName('texto')[0];    // en este caso seleccionamos el párrafo que tiene la clase 'texto' en la posición '0'

// Le asignamos el valor del 'id' al primer párrafo
// primerParrafo.id = 'primero';